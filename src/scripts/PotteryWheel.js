export const makePottery = (clayShape, clayWeight, clayHeight) => {
    let clayObject = {
        shape: clayShape,
        weight: clayWeight,
        height: clayHeight,
        id: 1
    }
    return clayObject
}
